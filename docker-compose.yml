name: Printyl Server

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - type: bind
        source: ./printyl
        target: /printyl
    env_file:
      - .env

volumes:
  server-data:
